name: weather_forecast
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  d_input: ^0.1.1 #untuk simple input
  d_view: ^0.0.3 #bantuan utilitas tampilan
  dartz: ^0.10.1 #memishkan flow error dan succes kiridan kanan
  equatable: ^2.0.5 #menyamakan pendeteksian properti
  extended_image: ^8.1.1 #image widget ketika error, loading spy maksimal
  flutter_bloc: ^8.1.3 #untuk state management
  get_it: ^7.6.4 #injeksi/ servis locater
  http: ^1.1.0 #akses ke network
  shared_preferences: ^2.2.2 #utk nyimpen data cache
  google_fonts: ^6.1.0 #font dgn tema default
  intl: ^0.18.1 #utk formating number or lainnyaa
  d_method: ^0.2.1 
  grouped_list: ^5.1.2 #buat list berdasar grouping

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  #flutter_lints: ^2.0.0
  build_runner: ^2.4.6
  mockito: ^5.4.2
  mocktail: ^1.0.1
  bloc_test: ^9.1.4 #testing state management

flutter:
  uses-material-design: true
  assets:
    - assets/ #folder yg menyimpan gambar yg diperlukan
